[project]
name = "vfm-engine-validation"
version = "0.1.0"
description = "Reproducible validation environment for VFM-engine"
requires-python = ">=3.13,<3.14"
dependencies = [
    "numpy>=2.2,<3",
    "scipy>=1.15,<2",
]

[tool.uv]
package = false
